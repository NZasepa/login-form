import { IsFieldFocusedDirective } from './is-field-focused.directive';

describe('IsFieldFocusedDirective', () => {
  it('should create an instance', () => {
    const directive = new IsFieldFocusedDirective();
    expect(directive).toBeTruthy();
  });
});
